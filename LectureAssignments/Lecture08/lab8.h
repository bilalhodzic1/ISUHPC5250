#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int factorial(int x);
double myexp(double x);
float *gen_set_of_points(float start_val, float interval, float end_val,
                         int *arr_size);
